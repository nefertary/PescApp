<%= form_for(@comment) do |f| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field" hidden>
    <%= f.label :user %><br>
    <%= f.text_field :user, value: @notice.user %>
  </div>
  <div class="field" hidden>
    <%= f.label :date %><br>
    <%= f.datetime_select :date, value: Time.now %>
  </div>
  <div class="field">
    <%= f.text_area :body %>
  </div>
  <div class="field" hidden>
    <%= f.label :image %><br>
    <%= f.text_field :image, value: "1.png" %>
  </div>
  <div class="field" hidden>
    <%= f.label :notice_id %><br>
    <%= f.text_field :notice_id, value: @notice.id %>
  </div>
  <div class="actions">
    <%= f.submit "Comentar", class: "btn blue btn-flat" %>
  </div>
<% end %>
